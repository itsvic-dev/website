const colors = require('tailwindcss/colors')

module.exports = {
	theme: {
		extend: {
			fontFamily: {
				sans: ['Rubik', 'Helvetica', '-system-ui'],
			},
			colors: {
				zinc: colors.zinc,
			},
		},
	},
}
