const Fallback = () => (
	<div class="bg-gradient-to-b from-slate-900 to-slate-800 flex justify-center items-center min-w-screen min-h-screen">
		<h1 class="text-5xl">Loading...</h1>
	</div>
)

export default Fallback
